import CheckPassword from "@/components/pages/mobile/CheckPassword";
import React from "react";

const page = () => {
  return (
    <div>
      <CheckPassword />
    </div>
  );
};

export default page;
